{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ObjectImage list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Filename</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('objectimage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.filename }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('objectimage_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('objectimage_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('objectimage_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
