{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OperationRecord list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Doat</th>
                <th>Act</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('operationrecord_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.doAt %}{{ entity.doAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.act }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('operationrecord_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('operationrecord_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('operationrecord_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
