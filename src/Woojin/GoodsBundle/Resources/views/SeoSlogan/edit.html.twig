{% extends '::bootstrap.html.twig' %}
{% block body -%}
    <h1>關鍵字列表</h1>

    {{ form(edit_form) }}

     <a href="{{ path('seoslogan') }}" class="btn btn-info">
        回到列表
    </a>
    {{ form(delete_form) }}
{% endblock %}
