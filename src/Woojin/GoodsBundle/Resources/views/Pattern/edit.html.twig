{% extends '::bootstrap.html.twig' %}

{% block body -%}
    {% if app.user|has_auth('PATTERN') %}
        <h1>{{ entity.name }}
            <small>
                <a href="{{ path('pattern') }}" class="btn btn-info">
                    回到列表
                </a>
            </small>
        </h1>

        {{ form(edit_form) }}

        {{ form(delete_form) }}
    {% else %}{% include 'WoojinStoreBundle:Common:warning.html.twig' %}{% endif %}
{% endblock %}
