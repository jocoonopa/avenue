{% extends '::bootstrap.html.twig' %}

{% block body -%}
    <h1>YahooCategory edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
    <li>
        <a href="{{ path('yahoocategory') }}">
            回到列表
        </a>
    </li>
   {#  <li>{{ form(delete_form) }}</li> #}
</ul>
{% endblock %}
